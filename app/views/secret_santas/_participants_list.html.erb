<div style="margin-top: 2rem;">
  <h4 style="margin-bottom: 1rem;">
    Participants (<%= @community.members.count %>)
  </h4>
  
  <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 0.5rem;">
    <% @community.members.each do |member| %>
      <div style="padding: 0.75rem; background: #f8f9fa; border-radius: 0.5rem; display: flex; justify-content: space-between; align-items: center;">
        <span><%= member_display_name(member) %></span>
        <% if assignment = @secret_santa.secret_santa_assignments.find_by(giver: member) %>
          <span style="font-size: 0.8rem; color: #28a745;">✓ Assigned</span>
        <% end %>
      </div>
    <% end %>
  </div>
</div>